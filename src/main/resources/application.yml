mybatis:
  mapper-locations: classpath:mappers/*xml
  type-aliases-package: com.example.demo1.mybatis.entity
server:
  port: 8080
spring:
  application:
    name: demo1
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: defaultDataSource
    password: xuehuafeiwu
    url: jdbc:mysql://localhost:3306/test_db?serverTimezone=UTC
    username: root
  thymeleaf:
    cache: true
    check-template: true
    check-template-location: true
    content-type: text/html
    enabled: true
    encoding: UTF-8
    excluded-view-names: ''
    mode: HTML5
    prefix: classpath:/templates/
    suffix: .html
